export type CarouselItem = {
  src: string;
  alt: string;
};

export type Button = {
  title: string;
  href: string;
  color: string;
  textColor: string;
  hoverColor: string;
  hoverTextColor: string;
};
