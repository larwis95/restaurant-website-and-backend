import { type ChartConfig } from "@/components/ui/chart";

const chartConfig = {
  morning: {
    label: "Morning",
    color: "#2563eb",
  },
  night: {
    label: "Evening",
    color: "#2563eb",
  },
} satisfies ChartConfig;

export { chartConfig };
